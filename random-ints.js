/**
 * Returns an array of unique random numbers
 *
 * @param n - the length of the returned array
 * @returns {number[]}
 */
function getUniqueArray(n) {
    // complete this function
}

/**
 * Generates a random integer between 0 and 9
 *
 * @returns {number}
 */
function generateRandom() {
    return Math.floor(Math.random() * 10)
}

module.exports = {
    getUniqueArray,
    generateRandom
}